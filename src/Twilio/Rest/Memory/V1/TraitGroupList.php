<?php

/**
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

namespace Twilio\Rest\Memory\V1;

use Twilio\Exceptions\TwilioException;
use Twilio\ListResource;
use Twilio\Options;
use Twilio\Stream;
use Twilio\Values;
use Twilio\Version;
use Twilio\ApiV1Version;
use Twilio\Http\Response;
use Twilio\Metadata\ArrayMetadata;
use Twilio\Metadata\PageMetadata;
use Twilio\Metadata\ResourceMetadata;
use Twilio\Metadata\StreamMetadata;
use Twilio\Serialize;


class TraitGroupList extends ListResource
    {
    /**
     * Construct the TraitGroupList
     *
     * @param Version $version Version that contains the resource
     */
    public function __construct(
        Version $version
    ) {
        $apiV1Version = new ApiV1Version($version->getDomain(), $version->version);
        parent::__construct($apiV1Version);

        // Path Solution
        $this->solution = [
        ];
    }

    /**
     * Helper function for Create
     *
     
     * @param ?TraitGroupRequest $traitGroupRequest
     * @return Response Created Response
     * @throws TwilioException When an HTTP error occurs.
     */
    private function _create(string $storeId, ?TraitGroupRequest $traitGroupRequest = null): Response
    {
        
        $uri = '/ControlPlane/Stores/' . \rawurlencode($storeId)
        .'/TraitGroups';
        
        $headers = Values::of(['Content-Type' => 'application/json', 'Accept' => 'application/json' ]);
        $data = $traitGroupRequest ? $traitGroupRequest->toArray() : [];
        return $this->version->handleRequest('POST', $uri, [], $data, $headers, "create");
    }

    /**
     * Create the CreateTraitGroup202ResponseInstance
     *
     
     * @param ?TraitGroupRequest $traitGroupRequest
     * @return CreateTraitGroup202ResponseInstance Created CreateTraitGroup202ResponseInstance
     * @throws TwilioException When an HTTP error occurs.
     */
    public function create(string $storeId, ?TraitGroupRequest $traitGroupRequest = null): CreateTraitGroup202ResponseInstance
    {
        $response = $this->_create($storeId, $traitGroupRequest);
        return new CreateTraitGroup202ResponseInstance(
            $this->version,
            $response->getContent(),
            $storeId
        );
        
    }

    /**
     * Create the CreateTraitGroup202ResponseInstance with Metadata
     *
     
     * @param ?TraitGroupRequest $traitGroupRequest
     * @return ResourceMetadata The Created Resource with Metadata
     * @throws TwilioException When an HTTP error occurs.
     */
    public function createWithMetadata(string $storeId, ?TraitGroupRequest $traitGroupRequest = null): ResourceMetadata
    {
        $response = $this->_create($storeId, $traitGroupRequest);
        $resource = new CreateTraitGroup202ResponseInstance(
                        $this->version,
                        $response->getContent(),
                        $storeId
                    );
        
        return new ResourceMetadata(
            $resource,
            $response->getStatusCode(),
            $response->getHeaders()
        );
    }


    /**
     * Reads TraitGroupInstance records from the API as a list.
     * Unlike stream(), this operation is eager and will load `limit` records into
     * memory before returning.
     *
     
     * @param string $storeId A unique Memory Store ID using Twilio Type ID (TTID) format
     
     * @param array|Options $options Optional Arguments
     * @param int $limit Upper limit for the number of records to return. read()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, read()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return TraitGroupInstance[] Array of results
     */
    public function read(string $storeId, array $options = [], ?int $limit = null, $pageSize = null): array
    {
        $this->uri = '/ControlPlane/Stores/' . \rawurlencode($storeId)
        .'/TraitGroups';
        return \iterator_to_array($this->stream($options, $limit, $pageSize), false);
    }

    /**
     * Reads TraitGroupInstance records from the API as a list
     * Unlike stream(), this operation is eager and will load `limit` records into
     * memory before returning.
     *
     
     * @param string $storeId A unique Memory Store ID using Twilio Type ID (TTID) format
     
     * @param array|Options $options Optional Arguments
     * @param int $limit Upper limit for the number of records to return. read()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, read()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return ArrayMetadata Array of results along with metadata
     */
    public function readWithMetadata(string $storeId, array $options = [], ?int $limit = null, $pageSize = null): ArrayMetadata
    {
        $this->uri = '/ControlPlane/Stores/' . \rawurlencode($storeId)
        .'/TraitGroups';
        $streamWithMetadata = $this->streamWithMetadata($options, $limit, $pageSize);
        $readResponse = \iterator_to_array($streamWithMetadata, false);
        return new ArrayMetadata(
            $readResponse,
            $streamWithMetadata->getStatusCode(),
            $streamWithMetadata->getHeaders()
        );
    }

    /**
     * Streams TraitGroupInstance records from the API as a generator stream.
     * This operation lazily loads records as efficiently as possible until the
     * limit
     * is reached.
     * The results are returned as a generator, so this operation is memory
     * efficient.
     *
     * @param array|Options $options Optional Arguments
     * @param int $limit Upper limit for the number of records to return. stream()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, stream()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return Stream stream of results
     */
    public function stream(array $options = [], ?int $limit = null, $pageSize = null): Stream
    {
        $pageSize = $options['pageSize'] ?? $pageSize;
        $limits = $this->version->readLimits($limit, $pageSize);

        $page = $this->page($options, $limits['pageSize']);

        return $this->version->stream($page, $limits['limit'], $limits['pageLimit']);
    }

    /**
     * Streams TraitGroupInstance records from the API as a generator stream and returns result with Metadata
     * This operation lazily loads records as efficiently as possible until the
     * limit
     * is reached.
     * The results are returned as a generator, so this operation is memory
     * efficient.
     *
     * @param array|Options $options Optional Arguments
     * @param int $limit Upper limit for the number of records to return. stream()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, stream()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return StreamMetadata stream of results with metadata
     */
    public function streamWithMetadata(array $options = [], ?int $limit = null, $pageSize = null): StreamMetadata
    {
        $pageSize = $options['pageSize'] ?? $pageSize;
        $limits = $this->version->readLimits($limit, $pageSize);

        $pageWithMetadata = $this->pageWithMetadata($options, $limits['pageSize']);

        $stream = $this->version->stream($pageWithMetadata->getPage(), $limits['limit'], $limits['pageLimit']);

        return new StreamMetadata(
            $stream,
            $pageWithMetadata->getStatusCode(),
            $pageWithMetadata->getHeaders()
        );
    }

    /**
     * Helper function for Page
     *
     * @param mixed $pageSize Number of records to return, defaults to 50
     * 
     * @return Response Paged Response
     */
    private function _page(
        array $options = [],
        $pageSize = Values::NONE
    ): Response
    {
        $options = new Values($options);

        $params = Values::of([
            'includeTraits' =>
                Serialize::booleanToString($options['includeTraits']),
            'pageSize' =>
                $options['pageSize'],
            'pageToken' =>
                $options['pageToken'],
            'orderBy' =>
                $options['orderBy'],
                                                
            
            'PageSize' => $pageSize,
        ]);

        $headers = Values::of(['Content-Type' => 'application/x-www-form-urlencoded', 'Accept' => 'application/json']);
        return $this->version->page('GET', $this->uri, $params, [], $headers);
    }

    /**
     * Retrieve a single page of TraitGroupInstance records from the API.
     * Request is executed immediately
     *
     * @param mixed $pageSize Number of records to return, defaults to 50
     * 
     * @return TraitGroupPage Page of TraitGroupInstance
     */
    public function page(
        array $options = [],
        $pageSize = Values::NONE
    ): TraitGroupPage
    {
        $response = $this->_page($options, $pageSize);

        return new TraitGroupPage($this->version, $response, $this->solution);
    }

    /**
     * Retrieve a single page of TraitGroupInstance records with metadata
     * Request is executed immediately
     *
     * @param mixed $pageSize Number of records to return, defaults to 50
     * 
     * @return PageMetadata of TraitGroupInstance
     */
    public function pageWithMetadata(
        array $options = [],
        $pageSize = Values::NONE
    ): PageMetadata
    {
        $response = $this->_page($options, $pageSize);

        $resource =  new TraitGroupPage($this->version, $response, $this->solution);

        return new PageMetadata(
            $resource,
            $response->getStatusCode(),
            $response->getHeaders()
        );
    }

    /**
     * Retrieve a specific page of TraitGroupInstance records from the API.
     * Request is executed immediately
     *
     * @param string $targetUrl API-generated URL for the requested results page
     * @return TraitGroupPage Page of TraitGroupInstance
     */
    public function getPage(string $targetUrl): TraitGroupPage
    {
        $response = $this->version->getDomain()->getClient()->request(
            'GET',
            $targetUrl
        );

        return new TraitGroupPage($this->version, $response, $this->solution);
    }


    /**
     * Constructs a TraitGroupContext
     *
     * @param string $storeId A unique Memory Store ID using Twilio Type ID (TTID) format
     *
     * @param string $traitGroupName A unique Name of the TraitGroup
     */
    public function getContext(
        string $storeId
        , string $traitGroupName
        
    ): TraitGroupContext
    {
        return new TraitGroupContext(
            $this->version,
            $storeId,
            $traitGroupName
        );
    }

    /**
     * Provide a friendly representation
     *
     * @return string Machine friendly representation
     */
    public function __toString(): string
    {
        return '[Twilio.Memory.V1.TraitGroupList]';
    }
}
