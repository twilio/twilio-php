<?php

/**
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

namespace Twilio\Rest\Memory\V1;

use Twilio\Exceptions\TwilioException;
use Twilio\ListResource;
use Twilio\Stream;
use Twilio\Values;
use Twilio\Version;
use Twilio\ApiV1Version;
use Twilio\Http\Response;
use Twilio\Metadata\ArrayMetadata;
use Twilio\Metadata\PageMetadata;
use Twilio\Metadata\ResourceMetadata;
use Twilio\Metadata\StreamMetadata;


class IdentifierList extends ListResource
    {
    /**
     * Construct the IdentifierList
     *
     * @param Version $version Version that contains the resource
     */
    public function __construct(
        Version $version
    ) {
        $apiV1Version = new ApiV1Version($version->getDomain(), $version->version);
        parent::__construct($apiV1Version);

        // Path Solution
        $this->solution = [
        ];
    }

    /**
     * Helper function for Create
     *
     
     
     * @param Identifier $identifier
     
     * @return Response Created Response
     * @throws TwilioException When an HTTP error occurs.
     */
    private function _create(string $storeId, string $profileId, Identifier $identifier): Response
    {
        
        $uri = '/Stores/' . \rawurlencode($storeId)
        .'/Profiles/' . \rawurlencode($profileId)
        .'/Identifiers';
        
        $headers = Values::of(['Content-Type' => 'application/json', 'Accept' => 'application/json' ]);
        $data = $identifier->toArray();
        return $this->version->handleRequest('POST', $uri, [], $data, $headers, "create");
    }

    /**
     * Create the CreateProfileIdentifier202ResponseInstance
     *
     
     
     * @param Identifier $identifier
     
     * @return CreateProfileIdentifier202ResponseInstance Created CreateProfileIdentifier202ResponseInstance
     * @throws TwilioException When an HTTP error occurs.
     */
    public function create(string $storeId, string $profileId, Identifier $identifier): CreateProfileIdentifier202ResponseInstance
    {
        $response = $this->_create($storeId, $profileId,  $identifier);
        return new CreateProfileIdentifier202ResponseInstance(
            $this->version,
            $response->getContent(),
            $storeId,
            $profileId
        );
        
    }

    /**
     * Create the CreateProfileIdentifier202ResponseInstance with Metadata
     *
     
     
     * @param Identifier $identifier
     
     * @return ResourceMetadata The Created Resource with Metadata
     * @throws TwilioException When an HTTP error occurs.
     */
    public function createWithMetadata(string $storeId, string $profileId, Identifier $identifier): ResourceMetadata
    {
        $response = $this->_create($storeId, $profileId,  $identifier);
        $resource = new CreateProfileIdentifier202ResponseInstance(
                        $this->version,
                        $response->getContent(),
                        $storeId,
                        $profileId
                    );
        
        return new ResourceMetadata(
            $resource,
            $response->getStatusCode(),
            $response->getHeaders()
        );
    }


    /**
     * Reads IdentifierInstance records from the API as a list.
     * Unlike stream(), this operation is eager and will load `limit` records into
     * memory before returning.
     *
     
     * @param string $storeId A unique Memory Store ID using Twilio Type ID (TTID) format
     
     * @param string $profileId The unique identifier for the profile using Twilio Type ID (TTID) format.
     
     * @param int $limit Upper limit for the number of records to return. read()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, read()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return IdentifierInstance[] Array of results
     */
    public function read(string $storeId, string $profileId, ?int $limit = null, $pageSize = null): array
    {
        $this->uri = '/Stores/' . \rawurlencode($storeId)
        .'/Profiles/' . \rawurlencode($profileId)
        .'/Identifiers';
        return \iterator_to_array($this->stream($limit, $pageSize), false);
    }

    /**
     * Reads IdentifierInstance records from the API as a list
     * Unlike stream(), this operation is eager and will load `limit` records into
     * memory before returning.
     *
     
     * @param string $storeId A unique Memory Store ID using Twilio Type ID (TTID) format
     
     * @param string $profileId The unique identifier for the profile using Twilio Type ID (TTID) format.
     
     * @param int $limit Upper limit for the number of records to return. read()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, read()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return ArrayMetadata Array of results along with metadata
     */
    public function readWithMetadata(string $storeId, string $profileId, ?int $limit = null, $pageSize = null): ArrayMetadata
    {
        $this->uri = '/Stores/' . \rawurlencode($storeId)
        .'/Profiles/' . \rawurlencode($profileId)
        .'/Identifiers';
        $streamWithMetadata = $this->streamWithMetadata($limit, $pageSize);
        $readResponse = \iterator_to_array($streamWithMetadata, false);
        return new ArrayMetadata(
            $readResponse,
            $streamWithMetadata->getStatusCode(),
            $streamWithMetadata->getHeaders()
        );
    }

    /**
     * Streams IdentifierInstance records from the API as a generator stream.
     * This operation lazily loads records as efficiently as possible until the
     * limit
     * is reached.
     * The results are returned as a generator, so this operation is memory
     * efficient.
     *
     * @param int $limit Upper limit for the number of records to return. stream()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, stream()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return Stream stream of results
     */
    public function stream(?int $limit = null, $pageSize = null): Stream
    {
        $limits = $this->version->readLimits($limit, $pageSize);

        $page = $this->page($limits['pageSize']);

        return $this->version->stream($page, $limits['limit'], $limits['pageLimit']);
    }

    /**
     * Streams IdentifierInstance records from the API as a generator stream and returns result with Metadata
     * This operation lazily loads records as efficiently as possible until the
     * limit
     * is reached.
     * The results are returned as a generator, so this operation is memory
     * efficient.
     *
     * @param int $limit Upper limit for the number of records to return. stream()
     *                   guarantees to never return more than limit.  Default is no
     *                   limit
     * @param mixed $pageSize Number of records to fetch per request, when not set
     *                        will use the default value of 50 records.  If no
     *                        page_size is defined but a limit is defined, stream()
     *                        will attempt to read the limit with the most
     *                        efficient page size, i.e. min(limit, 1000)
     * @return StreamMetadata stream of results with metadata
     */
    public function streamWithMetadata(?int $limit = null, $pageSize = null): StreamMetadata
    {
        $limits = $this->version->readLimits($limit, $pageSize);

        $pageWithMetadata = $this->pageWithMetadata($limits['pageSize']);

        $stream = $this->version->stream($pageWithMetadata->getPage(), $limits['limit'], $limits['pageLimit']);

        return new StreamMetadata(
            $stream,
            $pageWithMetadata->getStatusCode(),
            $pageWithMetadata->getHeaders()
        );
    }

    /**
     * Helper function for Page
     *
     * @param mixed $pageSize Number of records to return, defaults to 50
     * 
     * @return Response Paged Response
     */
    private function _page(
        $pageSize = Values::NONE
    ): Response
    {

        $params = Values::of([
            
            'PageSize' => $pageSize,
        ]);

        $headers = Values::of(['Content-Type' => 'application/x-www-form-urlencoded', 'Accept' => 'application/json']);
        return $this->version->page('GET', $this->uri, $params, [], $headers);
    }

    /**
     * Retrieve a single page of IdentifierInstance records from the API.
     * Request is executed immediately
     *
     * @param mixed $pageSize Number of records to return, defaults to 50
     * 
     * @return IdentifierPage Page of IdentifierInstance
     */
    public function page(
        $pageSize = Values::NONE
    ): IdentifierPage
    {
        $response = $this->_page($pageSize);

        return new IdentifierPage($this->version, $response, $this->solution);
    }

    /**
     * Retrieve a single page of IdentifierInstance records with metadata
     * Request is executed immediately
     *
     * @param mixed $pageSize Number of records to return, defaults to 50
     * 
     * @return PageMetadata of IdentifierInstance
     */
    public function pageWithMetadata(
        $pageSize = Values::NONE
    ): PageMetadata
    {
        $response = $this->_page($pageSize);

        $resource =  new IdentifierPage($this->version, $response, $this->solution);

        return new PageMetadata(
            $resource,
            $response->getStatusCode(),
            $response->getHeaders()
        );
    }

    /**
     * Retrieve a specific page of IdentifierInstance records from the API.
     * Request is executed immediately
     *
     * @param string $targetUrl API-generated URL for the requested results page
     * @return IdentifierPage Page of IdentifierInstance
     */
    public function getPage(string $targetUrl): IdentifierPage
    {
        $response = $this->version->getDomain()->getClient()->request(
            'GET',
            $targetUrl
        );

        return new IdentifierPage($this->version, $response, $this->solution);
    }


    /**
     * Constructs a IdentifierContext
     *
     * @param string $storeId A unique Memory Store ID using Twilio Type ID (TTID) format
     *
     * @param string $profileId The unique identifier for the profile using Twilio Type ID (TTID) format.
     *
     * @param string $idType Identifier type configured for the service (for example `email`, `phone`, or `externalId`).
     */
    public function getContext(
        string $storeId
        , string $profileId
        , string $idType
        
    ): IdentifierContext
    {
        return new IdentifierContext(
            $this->version,
            $storeId,
            $profileId,
            $idType
        );
    }

    /**
     * Provide a friendly representation
     *
     * @return string Machine friendly representation
     */
    public function __toString(): string
    {
        return '[Twilio.Memory.V1.IdentifierList]';
    }
}
